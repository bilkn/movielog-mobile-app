import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    flexDirection: "row",
  },

  image: {
    borderRadius: 10,
    height: 162,
    width: 110,
  },
  content: {
    marginLeft: 24,
  },
  controlsContainer: {
    flexDirection: "row",
    marginTop: 20,
  },
});
