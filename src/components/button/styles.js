import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    borderRadius: 250,
    justifyContent: "center",
    minWidth: 185,
    padding: 13,
  },

  iconButton: {
    alignItems: "center",
    borderRadius: 500,
    height: 40,
    justifyContent: "center",
    width: 40,
  },
});
