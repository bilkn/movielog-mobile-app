import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 15,
    width: "100%",
  },
  card: {
    alignItems: "center",
    borderRadius: 10,
    height: 70,
    justifyContent: "center",
    marginTop: 2,
    width: 70,
  },
  cardContainer: {
    alignItems: "center",
  },
});
