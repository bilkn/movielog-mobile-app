import React from "react";
import { MainLayout, Typography } from "../components";

const Home = () => {
  return (
    <MainLayout>
      <Typography variant="title">
        HOME PAGE
      </Typography>
    </MainLayout>
  );
};

export default Home;
