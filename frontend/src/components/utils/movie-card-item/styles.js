import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    flexDirection: "row",
  },

  image: {
    borderRadius: 10,
    height: 162,
    width: 110,
  },
  content: {
    marginLeft: 24,
    flex: 1,
  },
  controlsContainer: {
    alignItems: "center",
    flexDirection: "row",
    marginTop: 20,
  },
});
